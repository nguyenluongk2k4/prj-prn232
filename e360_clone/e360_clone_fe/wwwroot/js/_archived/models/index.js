/**
 * Index file - Import all models
 */

const ApiResponse = window.ApiResponse;
const PagedResponse = window.PagedResponse;
const PagedRequest = window.PagedRequest;
const ErrorResponse = window.ErrorResponse;

const Student = window.Student;
const Account = window.Account;
const UserInfo = window.UserInfo;
const Exam = window.Exam;

// Export
window.Models = {
    ApiResponse,
    PagedResponse,
    PagedRequest,
    ErrorResponse,
    Student,
    Account,
    UserInfo,
    Exam
};
