/**
 * Index file - Import all mappers
 */

const StudentMapper = window.StudentMapper;
const AccountMapper = window.AccountMapper;
const ExamMapper = window.ExamMapper;

// Export
window.Mappers = {
    StudentMapper,
    AccountMapper,
    ExamMapper
};
